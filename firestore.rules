rules_version = '2';

service cloud.firestore {
  match /databases/{database}/documents {
    // WARNING: These rules are completely open and are NOT secure.
    // They are intended for initial development and debugging ONLY.
    // Before deploying to production, you MUST implement secure rules.
    match /{document=**} {
      allow read, write: if true;
    }
  }
}
