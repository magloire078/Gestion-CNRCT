const admin = require('firebase-admin'); const serviceAccount = require('./serviceAccountKey.json'); if (!admin.apps.length) { admin.initializeApp({ credential: admin.credential.cert(serviceAccount) }); } async function run() { const snapshot = await admin.firestore().collection('employees').where('groupe_1', '==', 'Rois & Chefs').limit(1).get(); snapshot.forEach(doc => console.log(doc.data())); } run().catch(console.error);