// This script is disabled because the application is now using local mock data.
// No seeding is necessary.

function main() {
    console.log("Database seeding is disabled. The app uses local mock data from src/services/*.");
}

main();
