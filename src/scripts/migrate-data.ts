// This script is disabled because the application is now using local mock data.
// No migration is necessary.

function main() {
    console.log("Data migration is disabled. The app uses local mock data from src/services/*.");
}

main();
